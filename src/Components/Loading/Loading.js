import React from 'react';

const Loading = () => {
	return (
		<h1 className='flex justify-center items-center f-headline lh-solid white' style={{marginTop: '20%'}}>LOADING...</h1>
	);
}

export default Loading;